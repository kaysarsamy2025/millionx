LearnAI BD — Ready-to-Deploy Website (Vite + React)

1) Install dependencies (on your PC):
   - Open terminal in this folder
   - Run: npm install

2) Run locally:
   - npm run dev
   - Open the shown localhost URL

3) Build for hosting:
   - npm run build
   - This creates: /dist

4) Upload to your hosting:
   - Upload EVERYTHING inside the /dist folder to your hosting public_html (or htdocs)
   - Then open your domain.

Notes:
- This is a front-end prototype. You can connect your backend APIs later.
- If your hosting supports Node.js, you can also use: npm run preview
